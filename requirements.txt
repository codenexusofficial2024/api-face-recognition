# ====================================================================
# REQUIREMENTS FOR FACE RECOGNITION ATTENDANCE SYSTEM
# ====================================================================
# This file lists all the Python libraries required to run this project.
# To install them all, run: pip install -r requirements.txt
# ====================================================================


# -------------------------------
# API Framework (NEW)
# -------------------------------
# These are required to build and run the API servers (api_server.py and run.py)

# Modern, fast web framework for building APIs
fastapi>=0.100.0
# ASGI server for running FastAPI applications
uvicorn[standard]>=0.23.0


# -------------------------------
# Core Face Recognition Dependencies
# -------------------------------

# Face detection and recognition library
face-recognition>=1.3.0
# NOTE: dlib is a core dependency for face-recognition.
# It is highly recommended to install it via Conda to avoid build issues:
# conda install -c conda-forge dlib

# Numerical computations (matrix operations, linear algebra)
numpy>=1.19.5
# Image processing (opening, editing, converting images)
Pillow>=8.3.2
# Computer vision library (image/video reading, processing)
opencv-python>=4.5.0


# -------------------------------
# Networking / Requests
# -------------------------------

# HTTP requests made simple (for downloading student images from URLs)
requests>=2.28.0


# -------------------------------
# Optional & Utility Libraries
# -------------------------------
# The libraries below are not strictly required for the current api_server.py
# and run.py scripts but were in the original file. They can be useful for
# other tasks like video processing or building command-line tools.

# # Python library for video editing (cuts, concatenation, effects)
# moviepy>=1.0.3
# # Provides FFMPEG support for moviepy (handling video/audio codecs)
# imageio-ffmpeg>=0.4.5
# # Image input/output library (reading/writing different image formats)
# imageio>=2.13.0
# # Progress bar for long-running tasks
# tqdm>=4.62.0
# # Command-line utility helper
# click>=8.0.0